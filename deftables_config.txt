HubTable
(SensorId INTEGER PRIMARY KEY AUTOINCREMENT, HubSN INT NOT NULL, SensorType TEXT);
(HubSN, SensorType)
SampleTable
(SampleIndex INT PRIMARY KEY NOT NULL, SensorID TEXT NOT NULL,TimeStamp Text NOT NULL, RawData TEXT NOT NULL);
(SampleIndex, SensorID,TimeStamp, RawData)
CalibratedSampleTable
(SampleIndex INT PRIMARY KEY NOT NULL, CalibrationTimeStamp TEXT NOT NULL, CalVal TEXT NOT NULL, ModelNumber TEXT NOT NULL);
(SampleIndex, CalibrationTimeStamp, CalVal, ModelNumber)
CalModTable
(SensorId INT NOT NULL, ModelNumber INT NOT NULL, TYPE TEXT NOT NULL, Coefficient NOT NULL,PRIMARY KEY(SensorId,ModelNumber));
(SensorId, ModelNumber, Type, Coefficient)